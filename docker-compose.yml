services:
  ccp-bot:
    image: ghcr.io/xaverxd/ccp-bot:latest 
    container_name: ccp-bot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./database.yml:/app/config/database.yml